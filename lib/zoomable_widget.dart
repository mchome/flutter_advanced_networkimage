import 'package:flutter/material.dart';

class ZoomableWidget extends StatelessWidget {
  ZoomableWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return new Container();
  }
}
